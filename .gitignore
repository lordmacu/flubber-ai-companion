# Build output
# (anclado a la raíz: el filesystem de macOS es case-insensitive y "*.app/"
#  también ignoraba windows/Flubber.App/ — el proyecto C#)
/Flubber.app/
*.app.zip
*.dmg
dmg-stage/
# se regenera con dmg/make-bg.swift
dmg/background.png
build/
.DS_Store

# Datos locales del usuario viven en ~/Library/Application Support/SlimePet

icon/AppIcon.icns

# fuente del demo (se versiona el .mp4 ya convertido)
*.mov
